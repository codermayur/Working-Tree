# Backend API base URL (no trailing slash). For dev, leave unset to use Vite proxy: /api/v1 -> http://localhost:5005
# Market API: GET ${VITE_API_URL}/market/prices, /market/commodities, /market/states
VITE_API_URL=http://localhost:5005/api/v1

# WeatherUnion (Zomato) for Weather dashboard. Header: x-zomato-api-key. CORS: use Vite proxy /weather-api
VITE_WEATHER_UNION_KEY=your-weatherunion-api-key
# Fallback if you still use the old key name
# VITE_WEATHER_API_KEY=your-weatherunion-api-key
